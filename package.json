{
  "name": "json-to-json-mapper",
  "version": "1.0.0",
  "description": "Map Json to Json, using enum to cast object when converting id to string",
  "type": "commonjs",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "mocha -r ts-node/register ./tests/**/*.spec.ts"
  },
  "repository": {
    "type": "github",
    "url": "https://github.com/rmnunes/json-to-json-mapper"
  },
  "author": "Rodrigo Nunes",
  "license": "MIT",
  "dependencies": {
    "ts-loader": "2.2.2",
    "ts-node": "3.3.0",
    "typescript": "3.9.9"
  },
  "devDependencies": {
    "@types/chai": "^4.3.1",
    "@types/mocha": "^9.1.1",
    "@types/node": "^18.6.2",
    "chai": "^4.3.6",
    "mocha": "^10.0.0"
  }
}